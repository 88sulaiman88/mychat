# My Private AI Chat

واجهة دردشة شخصية تُنشر على Vercel، مع إبقاء مفتاح OpenAI في الخادم.

## متغيرات البيئة المطلوبة في Vercel

- `OPENAI_API_KEY`
- `SITE_PASSWORD`
- `OPENAI_MODEL` (اختياري، الافتراضي `gpt-5-mini`)

لا تضع مفتاح API داخل HTML أو JavaScript في مجلدd public.
